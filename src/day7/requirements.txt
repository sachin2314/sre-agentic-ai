# Core LangChain packages
langchain==0.3.0
langchain-openai==0.2.0
langchain-core==0.3.0

# OpenAI client (used by langchain-openai)
openai>=1.40.0

# Testing
pytest==8.3.2

# Standard library only used beyond here — no extra installs needed for:
# os, re, statistics, datetime, dataclasses, collections, time
